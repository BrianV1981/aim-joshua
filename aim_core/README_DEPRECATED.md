# DEPRECATED — use nested OS

Canonical engine path is now:

```text
aim-agy_os/.aim_core/
```

Do not add new modules here. This directory remains only as a temporary
backward-compat copy during lockstep migration. Prefer `./aim` which routes
to the nested core.
