# Smoke log — aim-joshua (updated 2026-07-25 with free Gemini key)

## Key fact for OpenCode + Google

OpenCode’s Google provider requires:

```bash
export GOOGLE_GENERATIVE_AI_API_KEY='…'
```

`GEMINI_API_KEY` / `GOOGLE_API_KEY` alone → `AI_LoadAPIKeyError: Google Generative AI API key is missing`.

## Results

| Test | Result |
|------|--------|
| Key file present | Yes (do not commit) |
| REST `gemini-3.5-flash-lite` | HTTP 200 `OK` |
| `./aim doctor` (non-sudo venv) | PASS |
| OpenCode full tree `aim-joshua` | **HANG** 120s, 0 bytes out |
| OpenCode **minimal** dir (copy of AGENTS.md + tiny README) | **PASS** text=`JOSHUA_MIN_OK` |
| bwrap + minimal + free Gemini | **PASS** text=`JOSHUA_BWRAP_OK` |

## Conclusion

1. Free Gemini + OpenCode **works** for JOSHUA-style boots.  
2. The **vessel repo** is DNA/template; **client runtime cwd should be thin** (like current aim-connect agent workspaces), not the whole git clone.  
3. aim-connect must inject **`GOOGLE_GENERATIVE_AI_API_KEY`**, not only `GEMINI_API_KEY`.

## Repro (minimal)

```bash
MIN=/tmp/joshua-min && mkdir -p "$MIN"
cp /home/kingb/aim-joshua/AGENTS.md "$MIN/"
export GOOGLE_GENERATIVE_AI_API_KEY='…'
opencode run --pure --format json --dir "$MIN" \
  -m google/gemini-3.5-flash-lite \
  "Reply with exactly: JOSHUA_MIN_OK"
```
